# dim(available.packages()) #패키지 개수 출력
# available.packages() #패키지 목록 출력
# sessionInfo() #Rsession 정보
# install.packages("") #패키지 설치
# library(stringr) #stringr패키지 로드 없을시 에러 발생
# require(stringr) #stringr패키지 로드 없을시 경고 발생
# search() #현재 로드된 패키지 목록 출력
# remove.packages("") #패키지 제거
